{
  "name": "my-reward-app",
  "version": "1.0.0",
  "main": "src/index.js",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "gh-pages": "^3.2.3"  // This is required for deploying to GitHub Pages
  },
  "devDependencies": {
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",  // Builds your app before deploying
    "deploy": "gh-pages -d build"  // Deploys the build folder to GitHub Pages
  },
  "homepage": "https://thealphakenya.github.io/simtwo", // Replace with your GitHub Pages URL
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
